# Base URL of the PathwayPilot backend.
#
# Leave unset to use production (https://pathwaypilot.ugent.be/api).
# Copy this file to `.env.local` and uncomment to develop against a
# backend running on your own machine:
#
#   cp .env.example .env.local
#
# VITE_API_BASE_URL=http://localhost:3000
